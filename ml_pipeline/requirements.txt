# ML Pipeline Requirements for TRL SFT Training with PyTorch
torch>=2.0.0
transformers>=4.21.0
datasets>=2.7.0
trl>=0.7.0
accelerate>=0.20.0
numpy>=1.21.0
peft>=0.4.0  # For LoRA if needed

# Optional: for quantization
bitsandbytes>=0.41.0

# SageMaker training (optional, for cloud deployment)
sagemaker>=2.100.0